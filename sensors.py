class Sensors:
  def humidity_status():

  def temperature_status():

  def is_temperature_between(min, max):
    temp = self.temperature_status
    return temp < max && temp > min

  def get_uv_light_power():
  
  def enable_uv_light_power(enable):
    if enable == True:
      print "UV lights on"
    elif enable == False:
      print "UV lights off" 
    else:
      print "[ERROR] " + str(enable) + " is not a recognized boolean."
  
  def get_heat_light_power():

  def enable_heat_light_power(enable):
    if enable == True:
      print "Heat lights on"
    elif enable == False:
      print "Heat lights off" 
    else:
      print "[ERROR] " + str(enable) + " is not a recognized boolean."
  
  def maintain_temperature_between(min, max):

  def set_lights_out(time):

  def failsafe():

